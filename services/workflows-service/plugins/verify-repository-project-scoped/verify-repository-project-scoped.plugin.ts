/* eslint-disable */
import verifyRepositoryProjectScoped from './verify-repository-project-scoped.rule';

const plugin = { rules: { 'verify-repository-project-scoped': verifyRepositoryProjectScoped } };

export default plugin;
